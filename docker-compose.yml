version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: uptime_postgres
    environment:
      POSTGRES_USER: rondon_admin
      POSTGRES_PASSWORD: rondon_pass123
      POSTGRES_DB: uptime_monitor_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

volumes:
  postgres_data:
